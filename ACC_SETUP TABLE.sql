CREATE TABLE ACC_SETUP 
   (	ID NUMBER GENERATED BY DEFAULT AS IDENTITY CONSTRAINT ACC_SETUP_PK PRIMARY KEY, 
	COMPANY_ID NUMBER, 
	ACC_CLIENT NUMBER, 
	ACC_PROVIDER NUMBER
    );
	
	 ALTER TABLE ACC_SETUP ADD CONSTRAINT ACC_SETUP_COMPANY_FK FOREIGN KEY (COMPANY_ID)
	  REFERENCES ADMIN_COMPANY (COMPANY_ID) ENABLE;